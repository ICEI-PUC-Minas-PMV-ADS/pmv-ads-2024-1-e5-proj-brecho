import { Avatar, Card, Rating, Typography } from "@mui/material";
import styled from "styled-components";

export const StyledReviewCard = styled(Card)({
  marginBottom: 20,
});

export const StyledReviewAvatar = styled(Avatar)({
  marginRight: 15,
});

export const StyledReviewRating = styled(Rating)({
  marginBottom: 10,
});

export const StyledReviewDescription = styled(Typography)({
  marginBottom: 10,
});
